// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Enable React's Strict Mode
    swcMinify: true, // Use the SWC compiler for faster builds
  };
  
  export default nextConfig;